# DSH 0.1.7-alpha.1 desktop compatibility

Version-pinned carry-forward of local session deletion, MP4 attachment routing and router-history compatibility. Preset registration and Host Cordis inspection use the new official APIs. The sidebar delete action uses the new menu slot. All patches retain strict before/after SHA-256 verification.
